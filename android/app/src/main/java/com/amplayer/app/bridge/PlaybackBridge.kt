package com.amplayer.app.bridge

class PlaybackBridge {
    fun attachPlayer() {
        // TODO: bind Media3 player lifecycle to Tauri bridge events.
    }

    fun emitState(state: String) {
        // TODO: send playback-state-changed events back to Rust/Tauri.
    }
}
